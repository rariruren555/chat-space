.side-ber
  .side-ber__user-name
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        =link_to new_group_path(current_user), class: "top-items__btn" do
          = icon('far', 'edit', class: "top-items__btn--icon")
        =link_to edit_user_path(current_user), class: "top-items__btn" do
          = icon('fa', 'cog', class: "top-items__btn--icon")

  .side-ber__group-list
    .group
      =link_to "#", class: "group a" do
        .group__name
          TECH CAMP
        .group__message
          まだメッセージはありません。